services:
  app:
    image: node:14
    container_name: co-work-backend
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    ports:
      - "3000:3000"
    command: npm start
    environment:
      - NODE_ENV=production
      - FIREBASE_PROJECT_ID=your-firebase-project-id
